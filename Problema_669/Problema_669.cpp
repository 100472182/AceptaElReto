#include <iostream>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    int n_varillas;
    while (std::cin >> n_varillas) {
        if (n_varillas == 0) {
            return 0;
        }
        int lamparas = 0;
    }
}